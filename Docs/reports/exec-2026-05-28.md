# Drift Daily Briefing — 2026-05-28

## Headline
Build 276 TestFlight shipped (11 unpublished commits, 26h since build 275); V7 Phase 3 Body design-doc now claimable after infrastructure unblocks; FM-chat-parity-gate decision locked in as phase 2b blocker per epic #860.

## Product Snapshot
Drift is an AI-first health tracker — users type what they ate or did, AI handles logging, tracking, and insights. On-device, no cloud. Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 276 (shipped 2026-05-28) |
| Test Suite | ~1219 iOS + ~913 DriftCore (~8s warm) + LLM eval |
| AI Capabilities | 35 registered tools + 11 analytical engines |
| Food Database | 5,420 (ceiling 6,000) |

## What Shipped This Period
- **Build 276 → TestFlight** — 11 commits from the queue landed (graphify graph rebuild, decisions doc entries for #860/#854 audits, stuck-detector threshold bump, watchdog hardening). First routine publish cycle after the senior queue unblock; clean back-to-back builds 275 → 276 validates the harness.
- **FM-chat-parity-gate decision locked (epic #860)** — Core insight: FM is now the *default* chat backend in production (Preferences.swift:251) but the cutover gate was never enforced (`testFoundationModelsChat_parityCutoverGate` skips unless env-var is set; preflight never sets it). Two design calls: (1) backend is a single global default, not per-route, so FM-below-bar means full revert only (no "revert one route"); (2) the gate must hard-fail loudly on unavailable FM hosts, never green-skip. Decision to enforce the gate as hard blocker before any FM-only code lands. Chosen over V7 Body (blocked #848) and exercise visuals (off-focus).
- **Decisions log pattern validated** — `userdefaults-leakage-cross-suite`, `queue-router-blocked-filter`, and `fm-chat-parity-gate-dormant` entries now on record with WHY framing. Future sessions designing new tests/routers have the shape to check against.

## What's Working Well
- **Infrastructure-first thinking is holding** — Instead of just patching bugs, the class-of-bug audit (#854) ensured the next queue router doesn't repeat the blocked-filter miss. The decisions doc is now the default reference for systemic shapes. Two-layer response (bug + class + doc entry) is the pattern.
- **Build cadence stabilized** — 26h cycle time, no archive failures, TestFlight recipe is deterministic and gated on clean state. The cooldown for archive-failed is working (no retry storms).
- **Harness discipline validated** — Single-epic lifecycle (#856 → 5 sub-tasks → drained → closed in <24h) proved the phase-2a/3a shape works. Spec-driven scoping + debate + implementation + landing = predictable throughput.

## Risks & Blockers
- **V7 Phase 3 Body design-doc (#848) is the main queue blocker** — Abandoned 4 times due to now-fixed upstream defects. Next senior claim is the 5th attempt. If it abandons again for a *new* reason, planning should reconsider scope — currently holds Body tab unification (WeightTabView + Rhythm + Glucose + Composition + Biomarkers), which is substantial surface area.
- **State.md still drifting (now 42-build gap: 234 → 276)** — Second consecutive daily call-out. Auto-routed routine-fix pattern hasn't fired. This is a visibility debt, not a product debt, but blocks confidence in the state snapshot.
- **Admin comment cadence still flat** — 6 consecutive daily reports, 0 admin engagement. Yesterday's question remains: continue daily, drop to 2-3x weekly, or ask-only format? One-line call closes the loop.

## Focus for Next Period
1. **Land V7 Phase 3 Body design-doc (#848)** — Senior queue head. Now unblocked after the infrastructure fixes. This is the gate for Body tab unification and the next implementation epic.
2. **FM-chat-parity-gate enforcement** — Enforce `DRIFT_FM_PARITY_GATE_STRICT=1` in preflight-check.sh, hard-fail (not skip) when FM unavailable on non-production hosts. This is the hard blocker before shipping any FM-only code paths.
3. **State.md snapshot refresh** — Update build number, test counts, and failing-queries. Either routine-fix or Part of #848 sub-task. Second ask; needs closure.

## Cost
| | |
|---|---|
| Model | Opus 4.7 (planning/senior cycles) |
| Sessions today | tracked in token-usage.sh |
| Est. cost today | tracked in token-usage.sh |
| Cost/cycle | tracked across cycles in token-usage.sh |

## Decision Needed
**Report cadence (2-day standing question):** 6 consecutive daily reports, 0 admin comments. Continue daily, drop to 2-3x weekly, or convert to ask-only (file only when a decision is needed)? One-line confirmation or explicit "keep daily".

---
Comment on any line for strategic feedback. @ashish-sadh
