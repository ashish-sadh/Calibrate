# Drift Daily Briefing — 2026-07-05

## Headline
The app is stable and shipping (TestFlight build 324), but the Drift Coach premium-feel polish arc (#909) is now blocked **6 days** on a single human-only infra fix — the iOS Simulator is unrunnable (#913). Everything the simulator doesn't gate keeps moving; the polish gate cannot.

## Product Snapshot
Drift is an AI-first health tracker — users type (or speak, or photograph) what they ate or did, and Drift Coach handles logging, tracking, and insights. On-device by default; Drift Coach runs on Nebius cloud (opt-in, surfaced). Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 324 (latest TestFlight) |
| Test Suite | ~1253 iOS + ~1552 DriftCore pure-logic tests; ~160 LLM eval cases |
| AI Capabilities | 35 tools (23 core + 12 insight) |
| Food Database | 5,420 foods (Indian-first) |
| Coach tool-routing | 48/50 (96%) on the Gemma gold set |

## What Shipped This Period
- **TestFlight cadence held — build 324 live** (up from 323 on 07-04). The 3-hourly publish path is fully unaffected by the simulator outage; archive/upload run without a booted sim.
- **Tier-0 (pure-logic) suite stays green** — Coach reliability hardening continues on the ~1552-test DriftCore path, which does not need the simulator.
- Honest note: **no new premium-feel/UI polish shipped** — that surface (#891 Tier-1 + `/ui-evaluator` v1b) is gated by #913 and cannot be verified until the sim runs. #891's code is complete and Tier-0-green, waiting on the sim to run its Tier-1 gate.

## What's Working Well
- **Drift Coach routing is solid** — 96% (48/50) on the tool-routing gold set; log_food / log_weight / mark_supplement at 100%.
- **The outage is well-contained** — Tier-0 tests and TestFlight archive continue, so autonomous sessions keep making progress on non-sim work instead of stalling entirely.
- **Privacy posture intact** — on-device default; the one cloud touchpoint (Coach on Nebius) stays explicitly opt-in.

## Risks & Blockers
- **#913 (P0, `needs-human`) — iOS Simulator unrunnable. THE single blocker, 6 days running.** CoreSimulator **1051.54 < Xcode 26.6's required 1051.55**; `xcrun simctl list devices available` → 0 devices (re-verified independently today at 09:41Z and again this planning cycle — the command hangs, 0 devices). This blocks **all Tier-1 (iOS-sim) tests**, `/ui-evaluator` visual scoring, and epic #909's premium-feel gate subtasks (#891, #898). Autonomous sessions **cannot** fix a system-level CoreSimulator version skew — it is inherently a human action.
- **Closure ≠ fix (history):** #913 was closed 06-30 but the sim never came up, so it was reopened 07-01 with exact fix steps and remains open + P0. Please confirm `simctl list devices available` returns devices *before* closing it this time.
- **Consequence:** the senior queue's premium-feel gate is frozen behind this P0, and #909's polish phase is correctly parked (not a scoping problem — re-scoping it would be churn). It lands the moment CoreSimulator is bumped.

## Focus for Next Period
1. **Unblock #913 (human).** This is the one action that reopens the entire polish arc and unfreezes the senior gate. Everything downstream waits on it.
2. **Keep hardening Coach reliability on Tier-0** — tool-routing whiffs, multi-turn context, clarification loops — the parts that don't need the simulator, each fix paired with a test.
3. **Hold TestFlight cadence** (every 3h) so beta reach doesn't regress while the gate is down.

## Cost
| | |
|---|---|
| Model | Opus (planning), Sonnet (senior/junior) |
| Sessions today | This planning session is early in the day; see `scripts/token-usage.sh --today` for the live count |
| Est. cost today | Minimal to date — the sim gate means no repeated sim-boot attempts burning cycles |
| Cost/cycle | Held down by working Tier-0 while the sim gate is blocked |

## Decision Needed
**#913 needs a human, and only a human.** Fix path (from the issue): launch Xcode.app once to run its first-launch install, or `sudo xcodebuild -runFirstLaunch` / `xcodebuild -downloadPlatform iOS`, or update macOS + reboot — whichever bumps the system CoreSimulator daemon to ≥ 1051.55 to match Xcode 26.6. **Verify `xcrun simctl list devices available` returns devices before closing #913** (the 06-30 closure didn't take). Until then the #909 polish phase stays parked — this is the honest state, not a re-scoping opportunity. One human action reopens the whole arc.

---
Comment on any line for strategic feedback. @ashish-sadh
