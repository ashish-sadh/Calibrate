# Drift Daily Briefing — 2026-07-04

## Headline
The app is stable and shipping (TestFlight build 323), but the Drift Coach premium-feel polish arc (#909) has now been blocked **5 days** on a single human-only infra fix — the iOS Simulator is unrunnable (#913). Autonomous work continues on everything the simulator doesn't gate.

## Product Snapshot
Drift is an AI-first health tracker — users type (or speak, or photograph) what they ate or did, and Drift Coach handles logging, tracking, and insights. On-device by default; Drift Coach runs on Nebius cloud (opt-in, surfaced). Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 323 (latest TestFlight) |
| Test Suite | ~1253 iOS + ~1552 DriftCore pure-logic tests; ~160 LLM eval cases |
| AI Capabilities | 35 tools (23 core + 12 insight) |
| Food Database | 5,420 foods (Indian-first) |
| Coach tool-routing | 48/50 (96%) on the Gemma gold set |

## What Shipped This Period
- **TestFlight cadence held** — build 323 live; the 3-hourly publish path is fully unaffected by the simulator outage (archive/upload run without a booted sim).
- **Tier-0 (pure-logic) suite stays green** — Coach reliability hardening continues on the ~1552-test DriftCore path, which does not need the simulator.
- Honest note: **no new premium-feel/UI polish shipped** — that surface (#891 Tier-1 + `/ui-evaluator`) is gated by #913 and cannot be verified until the sim runs.

## What's Working Well
- **Drift Coach routing is solid** — 96% (48/50) on the tool-routing gold set; log_food / log_weight / mark_supplement at 100%.
- **The outage is well-contained** — Tier-0 tests and TestFlight archive continue, so autonomous sessions keep making progress on non-sim work instead of stalling entirely.
- **Privacy posture intact** — on-device default; the one cloud touchpoint (Coach on Nebius) stays explicitly opt-in.

## Risks & Blockers
- **#913 (P0, `needs-human`) — iOS Simulator unrunnable. THE single blocker, 5 days running.** CoreSimulator **1051.54 < Xcode 26.6's required 1051.55**; `xcrun simctl list devices available` → 0 devices (re-verified today, 07:32Z). This blocks **all Tier-1 (iOS-sim) tests**, `/ui-evaluator` visual scoring, and epic #909's premium-feel gate subtasks (#891, #898). Autonomous sessions **cannot** fix a system-level CoreSimulator version skew — it is inherently a human action.
- **Consequence:** the senior queue is frozen behind this P0, and #909's polish phase is correctly parked (not a scoping problem — re-scoping it would be churn). It lands the moment CoreSimulator is bumped.

## Focus for Next Period
1. **Unblock #913 (human).** This is the one action that reopens the entire polish arc and unfreezes the senior queue. Everything else downstream waits on it.
2. **Keep hardening Coach reliability on Tier-0** — tool-routing whiffs, multi-turn context, clarification loops — the parts that don't need the simulator, each fix paired with a test.
3. **Hold TestFlight cadence** (every 3h) so beta reach doesn't regress while the gate is down.

## Cost
| | |
|---|---|
| Model | Opus (planning), Sonnet (senior/junior) |
| Sessions today | 0 counted so far (this planning session is early in the day) |
| Est. cost today | Minimal to date — see `scripts/token-usage.sh --today` |
| Cost/cycle | Held down by working Tier-0 while the sim gate is blocked (no repeated sim-boot attempts) |

## Decision Needed
**#913 needs a human, and only a human.** Fix path (from the issue): point `xcode-select` at an Xcode whose CoreSimulator ≥ 1051.55, or reinstall the matching Simulator runtime / Command Line Tools so the daemon version matches Xcode 26.6. Until then the #909 polish phase stays parked — this is the honest state, not a re-scoping opportunity. One human action reopens the whole arc.

---
Comment on any line for strategic feedback. @ashish-sadh
