# Drift Daily Briefing — 2026-06-19

## Headline
T-1 day to launch: this planning cycle scoped the **Drift Coach launch-hardening epic (#887)** and deferred the post-launch "Today's Brief" feature (#875) after verifying its narrator would run **unmeasured on Nebius** — the showstopper now gets a focused, bug-hunting push instead of new surface area.

## Product Snapshot
Drift is an AI-first health tracker — users type, speak, or photograph what they ate or did, and Drift Coach handles logging, tracking, and insights. Drift Coach now runs on **Nebius cloud** (on-device Gemma is the offline fallback). Closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 298 |
| Test Suite | ~1253 iOS + ~1552 DriftCore (pure-logic) + ~160 LLM eval cases |
| AI Capabilities | 35 tools (23 core + 12 insight) |
| Food Database | 5,420 foods (Indian-first, ceiling 6,000) |

## What Shipped This Period
- **Drift Coach moved to Nebius cloud** — chat now runs on a large cloud model (with on-device fallback), a step-change in reasoning quality for the showstopper.
- **Conversational food logging via text, voice, and "log my usual lunch"** — all routes now open one editable review sheet, parsed by Drift Coach.
- **Immersive voice talk-mode** — hands-free, dual-confirm voice loop with ElevenLabs studio voice (on-device fallback).
- **Granular backup progress** — live upload confirmation instead of a silent spinner.
- **Editable combo serving multiplier** — scale a logged combo meal up/down by serving.

## What's Working Well
- The autopilot harness self-corrected a classic trap cleanly: 4 of the deferred epic's 6 subtasks were already done on main; verify-at-file:line caught it instead of re-implementing.
- Conversational entry (text/voice/photo/"usual lunch") converging on a single editable sheet — consistent, trustworthy UX across every input mode.
- Drift Coach on Nebius gives the launch its showstopper: a genuinely capable cloud model, privacy-surfaced as an explicit cloud touchpoint.

## Risks & Blockers
- **Launch is T-1 day and the hardening epic was only scoped today.** #887's 7 subtasks (2 P0) must drain fast. The two P0s — coach tool-routing audit (#888) and "never load local Gemma when cloud configured" (#889) — gate the launch feel.
- **No client-side timeout on Nebius calls (#890).** A slow turn rides URLSession's 60s default → an indefinitely-lingering spinner, the most visible "feels broken" moment.
- **Offline / cloud-only first turn is undignified (#891).** A user who skipped the 2GB local download hits a bare error on their first message — their first impression of the showstopper.
- **`Docs/state.md` is stale** (says build 234; actual 298) — a manual snapshot that drifted; numbers above use authoritative sources.
- **Queue carries pre-pivot V7 cruft** (#833/#835/#847 visual refresh, #829 build-261 notes) that could mis-prioritize the dependency-unaware router at T-1 day; #834 (FM-backend multi-turn) closed as superseded this cycle.

## Focus for Next Period
1. **Drain #887's two P0s first** — tool-routing audit (#888) + never-load-local-Gemma (#889). *Why:* the showstopper must work flawlessly with every tool, and loading 2.9GB of unused Gemma on the cloud path is a launch-day memory/latency embarrassment.
2. **Premium-feel fixes** — Nebius request timeout (#890), offline first-turn dignity (#891), chat color/contrast across all chat cards (#893). *Why:* "premium feel = fast, no lingering spinners, correct contrast" is the launch bar.
3. **Lock the coach surface with tests** — multi-turn/clarification anti-loop (#892) + under-tested tools (#894). *Why:* every fixed failure must ship a test so launch hardening doesn't regress.

## Cost
| | |
|---|---|
| Model | Opus 4.8 (planning) |
| Sessions today | Not yet aggregated (token-usage.sh reported 0 at cycle 19118) |
| Est. cost today | See `scripts/token-usage.sh --today` |
| Cost/cycle | n/a |

## Decision Needed
- **Confirm the launch-hardening scope + the Today's Brief deferral.** The proactive brief engine is built and 100%-grounded *on Gemma*, but on the Nebius launch build that grounding is unmeasured (the #801 over-claim risk). Recommendation: keep it deferred until the grounding eval is re-run against Nebius post-launch. Agree?
- **Prune the pre-pivot V7 queue before launch?** Several visual-refresh / stale-build tasks remain unclaimed and could distract the router from #887 at T-1 day.

---
Comment on any line for strategic feedback. @ashish-sadh
