# Drift Daily Briefing — 2026-07-07

## Headline
**The 7-day blocker is cleared.** The iOS Simulator is back (CoreSimulator now 1051.55, `xcrun simctl list devices available` returns devices, #913 CLOSED) — and the Coach premium-feel epic #909 completed in full the moment it unblocked: all 7 subtasks closed, all 5 acceptance criteria re-verified against code. The next launch-hardening arc — killing UI-freeze/perf stalls (#1008) — is scoped and queued.

## Product Snapshot
Drift is an AI-first health tracker — users type (or speak, or photograph) what they ate or did, and Drift Coach handles logging, tracking, and insights. On-device by default; Drift Coach runs on Nebius cloud (opt-in, surfaced). Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 334 (version reconciled to App Store Connect) |
| Test Suite | ~1673 DriftCore + ~1253 iOS tests; ~160 LLM eval cases |
| AI Capabilities | 35 tools (23 core + 12 insight) |
| Food Database | 5,420 foods (Indian-first) |
| Coach tool-routing | 48/50 (96%) on the Gemma gold set |

## What Shipped This Period
- **iOS Simulator restored (#913 CLOSED).** CoreSimulator bumped 1051.54 → 1051.55; the human infrastructure action landed. Tier-1 iOS tests, `/ui-evaluator` visual scoring, and the whole sim-gated verification path are unblocked after 7 days.
- **Coach premium-feel epic #909 COMPLETE** — all 7 subtasks closed and re-verified against HEAD:
  - Bounded Nebius turn timeout so the chat can never hang indefinitely (#890).
  - Calm, retryable inline error (with an on-device-download chip) instead of a bare HTTP-code string when offline on the first turn (#891 — landed within hours of the sim returning).
  - Multi-turn context that holds + clarifications that don't loop (#892).
  - Multi-item Indian meals log one row per item — no dropped naan (#898).
  - Chat-surface colors are goal-aware and light-mode-legible (#893); English "log 500ml water" routes correctly (#901); under-tested coach tools now covered (#894).
- **Autonomous drain resumed** — junior + senior sessions cleared the polish backlog once the sim came up; every fix shipped with a regression test.

## What's Working Well
- **Single-blocker discipline paid off.** For 7 days the briefings named exactly one human action; when it landed, autonomous work drained the dependent backlog immediately with no re-planning.
- **The polish arc closed honestly** — the epic was held OPEN (not closed-to-dodge-the-watchdog) while its gate was unmet, then closed only after all 7 criteria verified in code.
- **On-device privacy posture holds** — default local, Nebius cloud opt-in only.

## Risks & Blockers
- **No human blocker today.** The sim outage that dominated the last week is resolved.
- **New P0 #977 (data integrity) — the new top correctness priority.** Calorie & macro targets *disagree across screens* for the same day (Dashboard 1,373 kcal / 162 g, coaching 168 g, Food diary 1,459 kcal / 160 g) because `macroTargets()` is called with three different weight bases. Numbers users trust should never contradict each other. Root-caused with file:line; a single-source-of-truth fix + guard test is queued top of the senior queue.
- **Six perf P1s (#970–975, #1007) — now the active arc (#1008).** Cycle opens with ~270 serial HealthKit queries; Dashboard ~86 per refresh; DEXA import / backup-restore / photo-log search / workout-history scroll each block the main thread. All confirmed against code; batched-query + off-main fixes scoped, each gated on a numeric-parity or off-main regression test.

## Focus for Next Period
1. **Fix #977 (data-integrity P0).** One weight basis for target math, routed through every surface; a Tier-0 guard so targets can't diverge again. *Why:* contradictory numbers undermine trust in every screen.
2. **Drain the perf-hardening epic #1008.** Start with the shared batched HealthKit helper (fixes Cycle + Dashboard + Sleep at one choke point). *Why:* "premium feel = fast, no jank" is the launch bar, and multi-second tab spinners are the most visible remaining stall.
3. **Hold TestFlight cadence + resume `/ui-evaluator` visual gates** now that the sim runs. *Why:* visual regressions were unscoreable for a week; catch up.

## Cost
| | |
|---|---|
| Model | Opus (planning), Sonnet (senior/junior), Haiku (TestFlight) |
| Sessions today | Planning (this) + autonomous drain of #909 subtasks |
| Est. cost today | Moderate — planning research (Explore + engineer/designer debate) + the resumed sim-gated drain |
| Cost/cycle | Normal cadence restored; no more doomed sim-gate retries burning sessions |

## Decision Needed
None blocking. FYI: with the sim restored, autopilot is back to full-throughput launch hardening — correctness (#977) first, then the perf arc (#1008). Flag if you'd rather reprioritize the perf arc ahead of #977, but the recommendation is correctness-first.

---
Comment on any line for strategic feedback. @ashish-sadh
