# Drift Daily Briefing — 2026-05-27

## Headline
Senior queue unblocked via epic #856 (tier-0 flake fix #852 + queue-router blocked-filter #853) — V7 Phase 3 Body design-doc claimable again after 4 abandonments; build 275 shipped overnight.

## Product Snapshot
Drift is an AI-first health tracker — users type what they ate or did, AI handles logging, tracking, and insights. On-device, no cloud. Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 275 (shipped 2026-05-27) |
| Test Suite | ~1219 iOS + ~913 DriftCore (~8s warm) + LLM eval |
| AI Capabilities | 35 registered tools + 11 analytical engines |
| Food Database | 5,420 (ceiling 6,000) |

## What Shipped This Period
- **Senior queue unblocked (epic #856 → #852 + #853)** — Two upstream infra defects had paralyzed the senior worker for ~3 days: (1) cross-suite `UserDefaults["drift_fm_food_intent_extract"]` race between two `@Suite(.serialized)` blocks made tier-0 flake → every senior bailed before claim; (2) `sprint-service.sh cmd_next` didn't filter `blocked` label, so the senior kept being re-handed the same parked issue every wake. Both fixed; V7 Phase 3 design-doc #848 now claimable cleanly.
- **TestFlight builds 274 + 275** — Two builds shipped overnight on top of build 273; carrier work is the infra unblocks, not new user-visible features. First clean back-to-back builds since the senior queue jam started.
- **Watchdog hardening** — 24h cooldown gate added for `testflight-archive-failed` (stops the auto-P0 from re-firing every cycle when the SDK fix is also blocked); `admin-replies` + `knowledge-curate` added to `USE_SKILLS` case (they were silently no-oping); stuck-detector no-comment-update threshold bumped 20 → 50 cycles (was firing spuriously during long design-doc work).
- **Planning loop fixes** — Planning now closes its tracking issue on exit (the 10-resume loop was orphaning issues), and stamps `in-progress-issue` on every claim/clear-on-done (visibility into who's working what without re-querying GitHub). Sprint-task label removed from epics — only sub-tasks are claimable, epic itself is pure scope container.
- **Class-of-bug audit (#854)** — Rather than just patching the queue-router gap, audited all queue-routing scripts for the same blocked-filter pattern; documented in `Docs/decisions.md` so the next router we add won't repeat the miss.

## What's Working Well
- **Two-layer defect response held** — Bug-instance fix (#852/#853) AND class-of-bug audit (#854) AND decisions-doc entry shipped same window. That's the right pattern when an incident reveals systemic shape, not just a one-off.
- **Decisions doc is the de-facto incident log** — `userdefaults-leakage-cross-suite` + `queue-router-blocked-filter` both landed with WHY-this-happened framing, not just what-was-changed. Future sessions reading the decisions doc will know to check for both shapes when designing new tests/routers.
- **Spec-driven epic shape validating** — Epic #856 was scoped → 5 sub-tasks filed → drained → closed cleanly in <24h. First full lifecycle test of the phase-2a/3a single-epic harness, and it worked.

## Risks & Blockers
- **6 consecutive daily reports with zero admin comments** (#848, #846, #844, #841, #816, #800) — same call-out as 2026-05-26. The "are these reaching anyone?" question is now a 3-cycle pattern; either drop cadence to 2-3x weekly, switch to ask-only format, or confirm subscribers are receiving them.
- **State.md still pinned at build 234 while live is 275** — 41-build drift, gap widening cycle over cycle. Yesterday's brief flagged this; nothing landed yet. Auto-routed routine-fix is the right shape but hasn't fired.
- **#848 abandonment debt** — V7 Phase 3 Body design-doc has been abandoned 4 times due to the now-fixed upstream defects. Next senior claim is the 5th attempt; if it abandons again for a NEW reason (not the GLP1/router stuff), planning should reconsider whether the scope is actually right.

## Focus for Next Period
1. **Land V7 Phase 3 Body design-doc (#848)** — Senior queue head, now unblocked. The doc is the gate for Body tab unification (WeightTabView + Rhythm + Glucose + Composition + Biomarkers). Without it, V7 Phase 3 is stalled.
2. **State.md + failing-queries snapshot refresh** — Build 275 truth; either as routine-fix or epic sub-task. Same item as yesterday, not auto-routing.
3. **Next single-epic arc (post-V7 Body design)** — Once the design-doc lands, scope the Body screen implementation as an epic following the proven #856 shape. Per tenet #0, also consider whether AI chat depth (FM extraction beyond chat, multi-turn reliability gate) should jump ahead of V7 Phase 3 implementation.

## Cost
| | |
|---|---|
| Model | Opus 4.7 |
| Sessions today | tracked in token-usage.sh |
| Est. cost today | tracked in token-usage.sh |
| Cost/cycle | tracked across cycles in token-usage.sh |

## Decision Needed
- **Daily report cadence** — Yesterday's brief raised this; 6 consecutive reports + 0 admin comments is now the pattern. Continue daily, drop to 2-3x weekly, or convert to ask-only (only file when there's a real decision needing input)? One-line confirmation closes the loop.

---
Comment on any line for strategic feedback. @ashish-sadh
