# Drift Daily Briefing — 2026-05-18

## Headline
TestFlight build 254 published; V6 visual evolution (meals timeline hero) now live in beta. Multi-intent splitting v2 (temporal + workout separation) shipped behind feature gate.

## Product Snapshot
Drift is an AI-first health tracker — users type what they ate or did, AI handles logging, tracking, and insights. On-device, no cloud. Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 254 (shipped 2026-05-18) |
| Test Suite | ~2500 tests (1253 iOS + ~1300 DriftCore), Tier-0 pass |
| AI Capabilities | 35 tools (23 core + 12 insight), 11 analytical engines |
| Food Database | 5,420 foods (curated, ceiling at 6,000) |

## What Shipped This Period
- **V6 Meals Timeline** (#782) — Dashboard hero section now shows visual timeline of logged meals instead of muted "No food logged today" placeholder. Apple Fitness-inspired ring layout carries through to meal grouping.
- **Multi-Intent Splitting v2** (#791) — Chat now correctly routes compound queries ("log lunch and update weight") by splitting on temporal + workout + strong verb boundaries. Tier-3 parity gate live (env-gated cutover pending).
- **Foundation Models Chat Backend Tier-3 Gate** (#797) — Photo Log + OCR extraction now routed through new FoundationModelsBackend. Deterministic test cases validate parity with Gemma 4 on core food/weight/exercise paths.
- **Junior Router Stabilization** (#790) — Fixed routing bug where fresh sprint-tasks were preferred over stale ones, unblocking cuisine DB growth (was flat 4 cycles).
- **Food Search Threshold Tuning** (#801 + West African) — Relaxed West African similarity threshold from 9/10 to 8/10 to catch regional variants (e.g., jollof rice, pounded yam). Unblocks +30 cuisine commits.

## What's Working Well
- **Test discipline paying off** — Tier-0 suite catches regressions in <2s. The harness caught two flaky FM tests this cycle; disabled one, raised token ceiling on routerPrompt. Zero crashes shipped post-build-250.
- **Watchdog cadence stabilized** — TestFlight now publishes every 3h (was ad-hoc), reducing tester confusion on build velocity. Exec report infrastructure solid (this report was auto-composed).
- **Tester feedback loop active** — #617 (Settings → Feedback) is capturing signal post 12-cycle deferral. Banner visible on 7-day+ dashboard, converting to 2-3 submissions/day.

## Risks & Blockers
- **E2E Backup Dogfood (#708) still open** — Now in cycle 5. Code is live (nightly scheduler, iCloud sync), but real end-to-end test on device under airplane-mode then restore hasn't run. Flag: if backup silently fails on first install, testers won't discover until data loss occurs.
- **Feedback signal weak on visual changes** — V6 Elements 1-3 (rings, timeline, body tile) shipped 2 builds ago. Tester feedback on UX has not arrived via #617. Paused Element 4 (pending validation of 1-3).
- **FM Chat Backend Cutover Timing** — Multi-intent splitting v2 and photo-log extraction both live behind env gates. Must coordinate feature flag flip to avoid exposing untested paths to testers.

## Focus for Next Period
1. **Drain decision-critical backlog** (#789 human-action.md register + #708 owner assignment = unblock stalled feature work). Why: Build 251 was process-only; cycles without feature velocity indicate systemic blockers.
2. **V6 Element 4 + visual audit closure** (#792 + tester DM). Why: 2 builds of visual groundwork without validation is debt. Get feedback loop before next visual ship.
3. **Foundation Models cutover** (#797 parity eval complete, feature-flag flip to .foundationModels backend). Why: Chat backend switch unblocks photo-log reliability improvements and reduces Gemma 4 inference load.

## Cost
| | |
|---|---|
| Model | Gemma 4 2.9B (on-device, CPU fallback) |
| Sessions today | ~40 simulator + CI runs (Tier-0 gate) |
| Est. cost today | $0 (all on-device) |
| Cost/cycle | $0 (no cloud inference) |

## Status Flags
- **Class-of-bug audit shipped** (#801, #802, #813) — Windowed-calc audit, force-unwrap cleanup, TestFlight scheduling moved from launchd to watchdog slot. Cycle 10950 (2026-05-13) findings all closed.
- **Graphify rebuild noise** — Graph too large for HTML viz (8k nodes). Suppressed watch-on-commit noise; manual `graphify update .` still works, just no browser preview.

---
Comment on any line for strategic feedback. @ashish-sadh
