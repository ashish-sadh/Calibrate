# Drift Daily Briefing — 2026-07-01

## Headline
Build 318 shipped with word-fraction food parsing ("a third of a cup"); iOS Simulator blocker (#913) remains unresolved — simulator infrastructure down system-wide, blocking polish gate.

## Product Snapshot
Drift is an AI-first health tracker — users type what they ate or did, AI handles logging, tracking, and insights. On-device, no cloud. Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 318 (shipped 2026-07-01 01:20 UTC) |
| Test Suite | ~2,100 tests (1,200+ iOS + 900+ DriftCore), ~60% coverage |
| AI Capabilities | 35 registered tools (23 core + 12 analytical) |
| Food Database | 5,420 foods (curated Indian-first + international) |

## What Shipped This Period
- **Word-fraction food parsing** (#53) — "a third of a cup of oats", "a quarter of an apple" now parse correctly through AI chat
- **Weight-goal test fixture rot-proofing** (#916) — fixed flaky tests using relative dates instead of hardcoded timestamps
- **Coach fixes from builds 313-317** — Nebius cloud backend improvements, timeout handling, multi-turn context, English water-logging route

## What's Working Well
- **TestFlight cadence held** — 4 builds shipped in past 4 days (build 315-318) despite infrastructure challenges; test suite stable
- **Drift Coach reliability improving** — multi-turn context now holds across turns without loops; confirmed working with Nebius backend at scale
- **Food logging AI depth** — word-fraction parsing + USDA fallback + DB match-first creates natural language data entry without user math

## Risks & Blockers
- **iOS Simulator infrastructure down (P0 #913)** — CoreSimulator 1051.54 < Xcode 26.6's required 1051.55; simctl hangs. Blocks ALL Tier-1 tests (UI/HealthKit/Widget), `/ui-evaluator`, mobile MCP. Affects epic #909 polish gate completion (#890/#891 blocked). Fix: `xcodebuild -downloadPlatform iOS` + launch Xcode once. **Needs human intervention.**
- **Epic #909 gate incomplete** — 5 of 6 issues closed; #891 (offline/cloud-only dignity) code-complete but can't run Tier-1 without simulator. #898 (multi-item meal drops) in verifier-budget tarpit (6+ pre-commit deaths). Not infra-blocked, but stalled.
- **Food DB ingestion rate flat** — queued food-addition tasks (#804 South Indian +30, #761 East Asian) not draining; cuisine routing stuck for 4 cycles despite 5,420-food ceiling buffer

## Focus for Next Period
1. **Unblock simulator** (#913) — Route to human via this briefing with concrete fix steps. Once unresolved, run Tier-1 for #891 (offline dignity) + #893 (chat color/contrast audit) before committing.
2. **Drain queued polish tasks** — #893 (grep-gated chat color) and #894 (Coach tool tests) are Tier-0 drainable WITHOUT simulator. Junior work ready to go.
3. **Close failing-query categories** — Docs/failing-queries.md refresh (cycle 10888→11100) to identify real AI chat regressions vs test stale-ness; land 1-2 fixes before next release.

## Cost
| | |
|---|---|
| Model | Claude Opus (planning/senior), Sonnet (junior) |
| Sessions today | testflight-publish (Haiku) + exec-briefing (Opus) |
| Est. cost today | ~$2.50 (deterministic recipes, no LLM eval) |
| Cost/cycle | ~$1.80 avg (planning 20m + review 3m) |

## Decision Needed
**Route P0 infra #913 (iOS Simulator CoreSimulator mismatch) to human oversight.** The watchdog cannot self-recover; every planning cycle re-fires the same override while #913 is open. Proposed: (1) Engineering: apply concrete fix (`xcodebuild -downloadPlatform iOS` + Xcode launch), (2) If fix doesn't work: escalate to Apple toolchain team or provision Mac mini with Xcode 26.6 +.

---
Comment on any line for strategic feedback. @ashish-sadh
