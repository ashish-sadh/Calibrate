# Drift Daily Briefing — 2026-07-06

## Headline
TestFlight build 326 shipped with #925 fix (Ladder Drill tracking type), but the iOS Simulator remains down (#913) — the premium-feel gate stays blocked. The single blocker is unchanged: CoreSimulator 1051.54 < Xcode 26.6 needs 1051.55 — this is a human-only infrastructure action.

## Product Snapshot
Drift is an AI-first health tracker — users type (or speak, or photograph) what they ate or did, and Drift Coach handles logging, tracking, and insights. On-device by default; Drift Coach runs on Nebius cloud (opt-in, surfaced). Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 326 (shipped today) |
| Test Suite | ~1673 DriftCore + ~1253 iOS tests; ~160 LLM eval cases |
| AI Capabilities | 35 tools (23 core + 12 insight) |
| Food Database | 5,420 foods (Indian-first) |
| Coach tool-routing | 48/50 (96%) on the Gemma gold set |

## What Shipped This Period
- **Build 326 → TestFlight (today).** Includes #925 fix: Ladder Drill now logs as a duration-tracked exercise (data-driven per-exercise TrackingType), not a reps exercise.
- **Tier-0 suite runs autonomously** — ~1673 DriftCore tests run every cycle, unblocked by the simulator outage.
- **TestFlight cadence maintained** — 3-hourly publish cycle holds; archive/upload do not require the simulator to be booted.

## What's Working Well
- **Coach reliability hardened** — 96% on tool-routing gold set (log_food/log_weight/mark_supplement all 100%).
- **Tier-0 test infrastructure resilient** — no sim dependency; autonomous work continues on the pure-logic/DriftCore path.
- **On-device privacy posture remains solid** — default local, Nebius cloud opt-in only.

## Risks & Blockers
- **#913 (P0, `needs-human`) — iOS Simulator unrunnable. Day 7 of outage.** CoreSimulator 1051.54 < Xcode 26.6's required 1051.55. `xcrun simctl list devices available` → hangs / 0 devices. Blocks all Tier-1 (iOS-sim) tests, `/ui-evaluator` visual scoring, and #909 premium-feel gate subtasks (#891, #898).
- **This is not a code problem** — autonomous sessions cannot fix system-level CoreSimulator version skew. Only human action (Xcode first-launch, macOS update, or explicit platform download) will bump the daemon to ≥1051.55.

## Focus for Next Period
1. **Unblock #913 (human action only).** Fix steps (from issue): Xcode.app first-launch install, or `sudo xcodebuild -runFirstLaunch` / `xcodebuild -downloadPlatform iOS`, or macOS update + reboot. **Verify `xcrun simctl list devices available` returns devices before closing.**
2. **Keep Tier-0 hardening** — Coach multi-turn context, clarification loops, tool-routing edge cases. Every fix paired with a test. This work does not need the simulator.
3. **Hold TestFlight cadence** — 3h publish cycle continues.

## Cost
| | |
|---|---|
| Model | Opus (planning), Sonnet (senior/junior) |
| Sessions today | N/A — testflight-publish is deterministic Haiku |
| Est. cost today | Minimal — no expensive sim-gate retries or heavy eval sweeps |
| Cost/cycle | Held down by working Tier-0 while the sim gate is blocked |

## Decision Needed
**#913 is a human action, no amount of code or autonomous work will resolve it.** The simulator daemon is pinned at 1051.54 and Xcode 26.6 requires 1051.55. One of: (a) Xcode first-launch setup, (b) `sudo xcodebuild -runFirstLaunch`, (c) macOS update + reboot. After the action, **please verify `xcrun simctl list devices available` returns device list** before marking resolved — the 2026-06-30 closure showed no recovery, so the verification step is critical.

---
Comment on any line for strategic feedback. @ashish-sadh
