# Drift Daily Briefing — 2026-06-28

## Headline
Drift Coach cloud response timeout bounded — eliminates indefinite spinners on slow Nebius responses, improving reliability perception.

## Product Snapshot
Drift is an AI-first health tracker — users type what they ate or did, AI handles logging, tracking, and insights. On-device, no cloud. Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 313 (shipped 2026-06-28 19:45 UTC) |
| Test Suite | ~2805 tests, ~65% coverage |
| AI Capabilities | 35 tools (23 core + 12 insight) |
| Food Database | 5,420 foods (Indian-first curated) |

## What Shipped This Period
- **Drift Coach cloud call now times out gracefully** — prevents indefinite spinners when Nebius is slow; surfaces retryable error instead of frozen UI
- **Graphify AST extraction** — post-commit knowledge graph rebuild for codebase indexing

## What's Working Well
- **Launch hardening momentum** — issue #890 closed on PASS, critical reliability fix shipped in single cycle
- **Dual-model stability** — on-device Gemma 4 baseline holding at 92.7% parity (FM relegated to opt-in after #872 NO-GO)
- **Test discipline** — Tier-0 hung-session mock in RemoteLLMBackendTests ensures timeout fires before hitting real delays

## Risks & Blockers
- **Nebius cloud latency** — root cause of timeout need is slow response times; latency SLA should be reviewed (timeout currently generous; repeated slow responses degrade UX even with graceful exit)
- **#890/#891 gate still open** — remaining launch-hardening tasks for Coach (#890 closed but #891 in review; #909 polish epic survival depends on both)
- **Docs stale** — state.md frozen at May 2026, build numbers out of sync; roadmap.md needs refresh for H2 deferred work visibility

## Focus for Next Period
1. **Land #891 (second Coach launch-gate)** — unblock #909 polish epic survivor list
2. **Hunt Coach tool-routing whiffs** — systematic test of every tool over voice/text/photo to surface missed routing edge cases
3. **Nebius latency baseline** — measure actual response times, set realistic timeout, consider region fallback

## Cost
| | |
|---|---|
| Model | Gemma 4 (on-device); Foundation Models opt-in |
| Sessions today | (TestFlight publish only; no session analytics) |
| Est. cost today | $0 (on-device + cloud opt-in) |
| Cost/cycle | $0 (privacy-first) |

---
Comment on any line for strategic feedback. @ashish-sadh
